


<table class="table">
   <tr> 
      <td  class="btn btn-success btn-block"><a href="<%=new_album_path%>" >add album</a></td>
   </tr>
    <tr>
        <th>album_name</th>
        <th>album_description</th>
        <th>action</th> 
    </tr>
        <% @album.each do|i| %>
        <tr><td><%=i.album_name%></td>
        <td><%=i.album_description%></td>
        <td> 

         <%= link_to "delete",album_path(i.id),:class=>:"btn btn-danger",method: :delete,data: { confirm: "Are you sure?" }%>
   
        <a href="<%=edit_album_path(i.id)%>" class="btn btn-info">edit</a>
        <a href="<%=album_path(i.id)%>" class="btn btn-info">show album</a>

        </td>
  </tr>
  <%end%>
  </table>
